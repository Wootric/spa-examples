import { WootricDirective } from './wootric.directive';

describe('WootricDirective', () => {
  it('should create an instance', () => {
    const directive = new WootricDirective();
    expect(directive).toBeTruthy();
  });
});
