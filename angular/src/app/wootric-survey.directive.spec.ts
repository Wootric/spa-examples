import { WootricSurveyDirective } from './wootric-survey.directive';

describe('WootricSurveyDirective', () => {
  it('should create an instance', () => {
    const directive = new WootricSurveyDirective();
    expect(directive).toBeTruthy();
  });
});
